apiVersion: v1
kind: Service
metadata:
    name: "testaks-11ee"
    labels:
        app: "testaks-11ee"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "testaks-11ee"